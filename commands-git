# Cloner un repository
git clone <repository_url>

# Les 3 commandes de push
git add .                # Ajoute tous les fichiers au staging
git commit -m "Message"  # Crée un commit avec un message
git push origin main     # Pousse les changements sur la branche main